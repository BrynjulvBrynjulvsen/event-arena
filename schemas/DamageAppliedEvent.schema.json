{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "io.practicegroup.arena.domain.DamageAppliedEvent",
  "title": "DamageAppliedEvent",
  "type": "object",
  "required": ["eventId", "eventType", "schemaVersion", "occurredAt", "matchId", "turn", "traceId", "payload"],
  "properties": {
    "eventType": { "const": "DamageApplied" },
    "payload": {
      "type": "object",
      "required": ["fighterId", "hpBefore", "hpAfter"]
    }
  }
}
