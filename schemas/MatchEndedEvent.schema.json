{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "io.practicegroup.arena.domain.MatchEndedEvent",
  "title": "MatchEndedEvent",
  "type": "object",
  "required": ["eventId", "eventType", "schemaVersion", "occurredAt", "matchId", "turn", "traceId", "payload"],
  "properties": {
    "eventType": { "const": "MatchEnded" },
    "payload": {
      "type": "object",
      "required": ["winnerFighterId", "loserFighterId", "reason", "winnerHpRemaining", "loserHpRemaining"]
    }
  }
}
